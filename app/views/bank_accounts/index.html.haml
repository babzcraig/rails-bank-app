%h1
  Bank Accounts

%table.table.table-bordered
  %thead
    %tr
      %th Account Number
      %th Balance
      %th Actions
    
  %tbody
    - @bank_accounts.each do |account|
      %tr
        %td #{account.account_number}
        %td #{number_to_currency(account.balance)}
        %td
          = link_to bank_account_path(account.id), class: "btn btn-primary" do 
            Show
